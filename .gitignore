*.DS_Store
.DS_Store
Thumbs.db
node_modules/
# Variables de entorno: el comodin cubre .env.production, .env.staging, .env.local.bak
# y cualquier variante futura. Sin el, un archivo nuevo con secretos entra al repo sin aviso.
.env*
!.env.example
.vscode/
playwright-report/
test-results/

# Claude Code workspace settings
.claude/

# MCP config (contiene el access token de Supabase — NUNCA commitear)
.mcp.json

# Supabase CLI local state
supabase/.temp/

# Prototipos / handoffs (no son la app real): son referencias de diseño en HTML que se
# recrean dentro del código, no se publican tal cual. El comodín cubre los que vengan.
GPS Builder/
GPS Chart Reference - handoff/
design_handoff_*/

# Notas y material de migración
checklist-pendientes.md
*.pptx

# Informe de auditoría de seguridad (describe vulnerabilidades — NUNCA commitear)
SECURITY-AUDIT.md

# Copias de seguridad locales (scripts/backup.mjs) — datos reales, NUNCA al repo
backups/

# Scratchpad y depuración: scripts sueltos que se escriben para mirar un dato una vez.
# No son la app y no tienen por qué sobrevivir a la sesión que los creó.
scratchpad/
scratchpad-*.mjs
_tmp-*.spec.js

# Capturas de prueba (impresión / html2canvas) que quedan en la raíz al depurar.
/*.png
deno.lock
